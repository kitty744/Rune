# Setting up the current Version (REPL)

After cloning the repository Into an IDE/Editor (Visual Studio code recommended), you can then open the terminal and run **go run ./src/repl** this will launch the programming language In REPL mode, which you can then test It.